---
interface Props {
  text?: string;
  className?:string
}

const { text , className } = Astro.props;
---

<small class={`text-secondary line-through font-semibold ${className}`}>
  {text}
</small>
